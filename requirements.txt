Django>=5.0,<5.1
python-dotenv>=1.0
whitenoise>=6.6        # statik dosyaları Django'nun kendisi servis eder
waitress>=3.0          # Windows üretim WSGI sunucusu

# Bu proje veritabanı KULLANMAZ; bu yüzden bir DB sürücüsüne gerek yoktur.
# İleride servise HTTP isteği atmak için 'requests' eklenebilir:
# requests>=2.31
